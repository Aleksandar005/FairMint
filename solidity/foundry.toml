[profile.default]
src = "src"
out = "out"
libs = ["lib"]
optimizer = true
optimizer_runs = 20000
via_ir = true
evm_version = "cancun"

# solc path: set to your local solc if needed

solc = "/usr/local/bin/solc"
